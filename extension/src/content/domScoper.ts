// Phase 1 stub: structured (Swagger) detection doesn't need a scoped section.
// Phase 2 grows this to scope generic prose docs by heading → next heading.

export function scopedHtml(_root: Element): string {
  return "";
}

export function authSectionHtml(): string {
  return "";
}
